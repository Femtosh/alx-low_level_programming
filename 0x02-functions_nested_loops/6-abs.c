#include "main.h"
/**
 * _abs - function calls for absolute number
 *
 * @c: is the int that will use for the argument
 * Return: Always 0 (Success)
 */
int _abs(int c)
{
	if (c > 0 || c == o)
	{
		return (c);
	}
	else
	{
		return (c * -1);
	}
}
