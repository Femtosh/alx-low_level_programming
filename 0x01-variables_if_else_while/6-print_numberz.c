#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int l;

	for (l = 48; l <= 57; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}

